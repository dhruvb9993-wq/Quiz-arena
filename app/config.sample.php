<?php
/**
 * QuizArena — SAMPLE configuration file (REFERENCE ONLY).
 *
 * DO NOT EDIT AND DO NOT RENAME THIS FILE.
 *
 * The real configuration file  app/config.php  is generated automatically
 * by the browser installer (install.php) when you first open the site.
 * To re-install on a new Hostinger database, simply delete app/config.php
 * via the Hostinger File Manager and open install.php again.
 *
 * This sample only documents the structure the installer writes:
 *
 * <?php
 * if (!defined('QA_RUNNING') && !defined('QA_INSTALLING')) { exit('Direct access denied'); }
 * return array(
 *   'db_host'      => 'localhost',
 *   'db_port'      => '3306',
 *   'db_name'      => '********',
 *   'db_user'      => '********',
 *   'db_pass'      => '********',
 *   'base_url'     => 'https://yourdomain.com',
 *   'timezone'     => 'Asia/Kolkata',
 *   'debug'        => false,
 *   'installed_at' => 'YYYY-MM-DD HH:MM:SS',
 * );
 *
 * NOTE: app/config.php is protected from web access by app/.htaccess and
 *       is ignored by Git so credentials can never be pushed to GitHub.
 */
if (!defined('QA_RUNNING') && !defined('QA_INSTALLING')) { exit('Direct access denied'); }
return null; // inert — never loaded by the application
